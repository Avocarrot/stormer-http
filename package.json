{
  "name": "stormer-http",
  "version": "0.3.0",
  "description": "Implementation of Stormer Store for HTTP clients",
  "main": "index.js",
  "repository": "git@github.com:Avocarrot/stormer-http.git",
  "author": "avocarrot",
  "license": "MIT",
  "engines": {
    "node": ">=6.10.2"
  },
  "scripts": {
    "pretest": "eslint --ignore-path .gitignore .",
    "test": "find ./test -name '*.spec.js' | xargs tape NODE_ENV=test",
    "cov": "find ./test -name '*.spec.js' | xargs istanbul cover tape NODE_ENV=test"
  },
  "devDependencies": {
    "eslint": "^3.15.0",
    "stormer-memcached": "0.0.3",
    "tape": "^4.6.3",
    "tape-nock": "^1.6.0"
  },
  "dependencies": {
    "debug": "^2.6.1",
    "request": "^2.81.0",
    "stormer": "^0.10.0",
    "util": "^0.10.3"
  }
}
